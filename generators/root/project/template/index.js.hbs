'use strict';

require('initialize');
require('file?name={{dasherize name true}}/index.html!./index.html');
require('./index.less');
var React = require('react');
var app = require('./app');
var routes = require('./routes');

React.renderComponent(app(), document.body);
routes.start();
