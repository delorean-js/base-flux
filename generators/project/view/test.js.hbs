/** @jsx React.DOM */
'use strict';

require('initialize');
var TestUtilities = require('react/addons').TestUtilities;
var {{uppercamelize name true}}View = require('./index');

define('{{humanize name}} view', function() {
  it('', function() {
  });
});
