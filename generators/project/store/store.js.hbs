'use strict';

var Rx = require('rx');
var dispatcher = require('lib/dispatcher');
var observable = dispatcher.ofStore('{{dasherize name true}}');
var subject = new Rx.Subject();

module.exports = subject.toStore();

// observable.ofAction('action').subscribe(...);
