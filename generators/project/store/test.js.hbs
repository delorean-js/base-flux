'use strict';

require('initialize');
var Rx = require('rx');
var dispatcher = require('lib/dispatcher');
var {{camelize name true}}Store = require('./index');

describe('{{humanize name}} store', function() {
  it('', function() {
  });
});
