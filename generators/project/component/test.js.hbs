/** @jsx React.DOM */
'use strict';

var TestUtilities = require('react/addons').TestUtilities;
var {{uppercamelize name true}} = require('./index');

define('{{humanize name}} component', function() {
  it('', function() {
  });
});
